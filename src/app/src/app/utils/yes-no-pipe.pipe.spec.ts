import { YesNoPipePipe } from './yes-no-pipe.pipe';

describe('YesNoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new YesNoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
